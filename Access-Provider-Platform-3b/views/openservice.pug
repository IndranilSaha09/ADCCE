doctype html
head
 link(rel="icon", href="/images/favicon.ico")
 style.
  body { 
    margin: 0;  
    padding: 0;  
    background-color:#fff066;
    font-family: 'Arial';  
  } 
  table, th, td {
  border:1px solid blue;
  }
h1.text-center.top-buffer Access Management Platform   
h1.text-center.top-buffer #{title}
table(style='width:100%')
  tr
    th Agreement Title
    th Domain
    th Technology 
    th Role
    th Select to offer
  each detail in details
    tr
    td #{detail.agreement_title}
    td #{detail.domain}
    td #{detail.technology}
    td #{detail.role}
    td 
     li
      a(href=`/users/offerEmployee?agreement_title=${detail.agreement_title}&domain=${detail.domain}&technology=${detail.technology}`) OfferEmployee
    td 